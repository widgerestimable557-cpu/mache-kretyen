// chatbot-config.js

// AI Chatbot Configuration for Classic Curriculum Setup

const chatbotConfig = {
    interface: 'floatingModal',
    gradingDistinction: 'maternal',
    autoConfigPreview: true,
};

module.exports = chatbotConfig;
